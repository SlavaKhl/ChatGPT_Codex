# Решения

- 2026-08-29: Контекст проекта хранится в `./.agent_memory/` по протоколу FSDB.
- 2026-08-30: Для подключения внешней базы знаний выбран проектный `.codex/config.toml` и STDIO-пакет `@modelcontextprotocol/server-filesystem`, чтобы настройка действовала только в текущем проекте.
- 2026-08-30: Состояние MCP `filesystem` проверять вызовом его инструментов (`list_allowed_directories`, `list_directory`), а не через `resources/list`, поскольку сервер может не публиковать статические ресурсы.
- 2026-09-01: GitHub исправляется новым коммитом поверх существующей ветки `main`, без force-push: ошибочные вложенные каталоги удаляются, локальное содержимое проекта размещается в корне репозитория.
